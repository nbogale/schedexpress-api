import { RequestStatus, RequestPriority, NotificationType, ConflictType, RuleType, UserRole as UserRoleType} from '@prisma/client';

// Re-exporting the Prisma enums to maintain API compatibility
export { RequestStatus, RequestPriority, NotificationType, ConflictType, RuleType, UserRoleType};
